import 'package:flutter/material.dart';

class Maternityclothingsection extends StatelessWidget {
  const Maternityclothingsection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("maternityclothing"),
    );
  }
}