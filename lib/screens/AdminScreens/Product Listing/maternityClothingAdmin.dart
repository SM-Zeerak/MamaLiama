import 'package:flutter/material.dart';

class MaternityclothingAdmin extends StatelessWidget {
  const MaternityclothingAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("maternityclothing"),
    );
  }
}